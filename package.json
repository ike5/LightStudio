{
  "name": "lightstudio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Photo editor with draggable light-source orbs and per-region relighting",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
